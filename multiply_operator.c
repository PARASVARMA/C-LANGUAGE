This is how a multiply operator works specially multiply operator:-
#include <stdio.h>

int main() {
  int length = 10;
  int width = 5;
  int area;

  area = length * width;
  printf("%d \n", area);  /* 50 */

  return 0;
}
